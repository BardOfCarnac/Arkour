# Runner glyph presets

- `runner-glyph-v1.json` is the exact 2D preset supplied after the first touch-editor pass.
- The 3D editor promotes v1 points to `{x, y, z}` with `z: 0`; the original file remains unchanged as a recoverable design checkpoint.
