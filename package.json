{
  "name": "arkour",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node --check public/route-first-city/runner-view.js && node --check public/runner-pose-lab/app.js && node --check public/runner-pose-lab/mobile-drawer.js && node --check public/black-ice-glyph-editor/editor.js && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.179.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.1",
    "@types/three": "^0.179.0"
  }
}
